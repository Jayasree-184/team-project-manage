# Landing verification

The public root route renders the PROJECT CONTROL SYSTEM hero, concrete architectural Canvas, technical status readouts, seven scene sections, and dashboard CTAs. The authenticated `/app` route still resolves to the existing Team Member dashboard with scoped task controls and no route regression. The full suite passes with 31 tests, TypeScript validation passes, and the production build succeeds. Landing-specific tests cover the hero/scene markers and separation of `/` from protected `/app` routes.
